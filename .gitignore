# Node modules
node_modules/

# Lock files (optional, but bun.lock should be committed or ignored based on preference. We keep bun.lock tracked since it's standard for packages, but let's ignore local system logs/caches)
.replit
replit.nix

# System files
.DS_Store
Thumbs.db

# Log files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Biome / tool caches
.biome/
